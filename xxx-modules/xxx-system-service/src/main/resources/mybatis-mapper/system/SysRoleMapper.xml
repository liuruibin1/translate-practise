<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xxx.system.mapper.SysRoleMapper">

    <sql id="querySelect">
        <![CDATA[
            SELECT tall.* FROM sys_role tall
        ]]>
    </sql>

    <sql id="queryWhere">
        <where>
            <!-- id -->
            tall.id IS NOT NULL

            <!-- id -->
            <if test="paramObj.id != null">
                AND tall.id = #{paramObj.id}
            </if>

            <!-- name -->

            <!-- sort -->

            <!-- is_enabled -->
            <if test="paramObj.isEnabled != null">
                AND tall.is_enabled = #{paramObj.isEnabled}
            </if>

            <!-- update_ts -->

            <!-- 其它字段 -->

            <if test="paramObj.key != null and !paramObj.key.isEmpty()">
                <![CDATA[
                    AND (
                            LOWER(tall.name) LIKE CONCAT('%', LOWER(#{paramObj.key}), '%')
                        )
                ]]>
            </if>

        </where>
    </sql>

    <sql id="queryOrderBy">
        ORDER BY tall.id DESC
    </sql>

    <select id="queryPage" parameterType="java.util.Map" resultType="com.xxx.system.vo.SysRoleVO">
        <include refid="querySelect"/>
        <include refid="queryWhere"/>
        <include refid="queryOrderBy"/>
    </select>

    <select id="queryList" parameterType="java.util.Map" resultType="com.xxx.system.vo.SysRoleVO">
        <include refid="querySelect"/>
        <include refid="queryWhere"/>
        <include refid="queryOrderBy"/>
    </select>

</mapper>